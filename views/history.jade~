each his in histories
    .cmgroup                            
        .grpheader #{his.date}
        -for(var i = 0; i < his.cms.length; i++) {
            -var cm = his.cms[i];
            -if (i == his.cms.length - 1)
                .grpbody
            -else
                .grpbodyinner
                    a.comment(href=cm.fileurl) #{cm.comment}
                    br
                    a.cmuser(href='#') #{cm.user}
                    span.cmdate #{cm.date}
        -}
